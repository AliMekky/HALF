# Notebooks

The complete historical notebook is preserved at `../legacy/data_analysis.ipynb`, including saved outputs (credential strings redacted). It is exploratory and is not safe to Run All: cells can submit API jobs or overwrite artifacts. Its historical absolute paths remain in this archival copy. Use extracted functions in `llmbias.analysis` for new work, with explicit input/output paths.

Cell provenance is in `docs/module-map.json`. Extracted functions retain historical behavior and, where present, dependencies on module globals. See `docs/analysis.md` for usage. No notebook execution was performed during migration.
